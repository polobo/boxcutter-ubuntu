{
  "_comment": "Build with `packer build -var-file=ubuntu{version}.json -var-file=ubuntu-add-desktop.json ubuntu.json`",
  "vm_name": "ubuntu-desktop",
  "desktop": "true",
  "vagrantfile_template": "tpl/vagrantfile-ubuntu-desktop.tpl"
}
